#include <WiFi.h>

void setup() {
  Serial.begin(115200);

  // Set ESP32 to Station mode
  WiFi.mode(WIFI_STA);

  delay(1000);

  Serial.println();
  Serial.println("ESP32 MAC Address:");
  Serial.println(WiFi.macAddress());
}

void loop() {
  // Nothing to do
}